<% if obj.errors.any? %>
    <h2>The following prevented the data from being saved</h2>
    <ul>
    <% obj.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
    </ul>
    <% end %>
<% end %>